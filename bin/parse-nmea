#!/usr/bin/env node

import Liner from "../liner.js";
import {createDefaultTransformer} from "../extended-nmea.js";
import {stringify} from "JSONStream";

process.stdin.resume();
process.stdin.setEncoding('utf8');

process.stdin.pipe(new Liner()).pipe(createDefaultTransformer()).pipe(stringify(false)).pipe(process.stdout);
